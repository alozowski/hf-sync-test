"""Configuration file - should be tracked"""

class Config:
    APP_NAME = "HF Sync Test"
    VERSION = "1.0.0"
    PORT = 7860
    
    # Note: Actual secrets should be in .env (which is gitignored)
    # This file only contains non-sensitive configuration
